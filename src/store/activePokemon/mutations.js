export const mutations = {
    pushActivePokemon(state, pokemon) {
        state.activePokemon = pokemon
    },
    setPokemonCharacteristic(state, characteristic) {
        state.pokemonCharacteristic = characteristic
    }
}

